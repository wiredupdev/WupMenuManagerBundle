{% macro renderMenu(menu) %}
    {% import _self as self %}
    {% if menu.hasChildren %}
<ul id="{{ menu.id }}">
    {% for item in menu %}
        {% if item.isVisible %}
            <li>
                <a href="{{ item.getUri() }}" target="{{ item.getTarget() }}" >{{ item.label }}</a>
                {% if item.hasChildren %}
                    {{ self.renderMenu(item) }}
                {% endif %}
            </li>
        {% endif %}
    {% endfor %}
</ul>
{% endif %}
{% endmacro %}
{% from _self import renderMenu %}

{{ renderMenu(menu) }}
